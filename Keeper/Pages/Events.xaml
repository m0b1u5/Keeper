<Page x:Class="Keeper.Pages.Events"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Keeper.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Events">

    <Border Background="#eff2f7" CornerRadius="50">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="EVENTS" FontFamily="ubuntu" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="#001122" FontSize="60" FontWeight="Bold" Margin="10,10,10,10"/>
            <StackPanel Grid.Column="1">
                <Button x:Name="pops" Margin="0,5,0,0" Click="pops_Click" FontFamily="ubuntu" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="#001122" FontSize="60" FontWeight="Bold">
                    DB
                </Button>
            </StackPanel>
        </Grid>
       


    </Border>



</Page>
